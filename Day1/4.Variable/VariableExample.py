#variable are user defined so a variable can be named anything
"""
althought there are rules to declare a variable which we will 
stydy later but for now lets name a variable according to 
operation since we will be taling user name as input we will 

use variable "name"  """


name=input("Enter your name:")
print(name)